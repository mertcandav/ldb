name: Test
on: [push, pull_request]

jobs:
  build-and-exec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: julelang/setup-jule@v2
        with:
          version: dev

      - name: Implementation Tests
        run: |
          julec test -o ldbtest .
          ./ldbtest

      - name: Use Tests
        run: |
          julec test -o ldbtest test
          ./ldbtest
